# Active cutover — 2026-09-14

## Status

The acceptance gate passed and `pi-memory-orchestrator` is active for Pi and OMP.

- Active bank: `coding-agent::dudong2`
- Database: local Hindsight PostgreSQL
- Reranker: OpenRouter `cohere/rerank-v3.5`, RRF fallback
- Bounded layer: official `pi-hermes-memory@0.9.9` in `legacy-inject` mode
- Former official per-repository Hindsight adapters: disabled
- Original banks and backup archives: preserved

## Evidence

- Shadow acceptance: 51 turns, 10 correction chains, 100/100 recall queries, P95 176 ms.
- Migration inventory: 201 documents classified; 30 documents and 316 raw facts imported; 163 preserved-only; 8 rejected as duplicate/marker content; one invalidated memory preserved for audit.
- Final scoped recall: LuckyCat no-fill, LuckyCat walking, orchestrator, and memory-workspace cases all passed in 261–377 ms.
- Pi active automatic recall answered the OMP-authored cross-harness fact correctly.
- Pi- and OMP-authored final-bank facts retain `harness: pi` and `harness: omp` provenance respectively.
- Production outbox reached 0 pending, 0 processing, 0 failed.

Full machine-readable evidence:

`~/.hindsight/hindsight-backups/pi-memory-orchestrator-20260914T045259Z/cutover-report.json`

## Temporary migration mode

Final-bank consolidation continues in the background. Until `pending_consolidation` reaches zero, active recall uses:

```json
{
  "recallTypes": ["observation", "world", "experience"],
  "preferObservations": true
}
```

This prefers completed observations while keeping unconsolidated imported facts queryable. After the backlog reaches zero, change to observation-only recall.

## Residual external blocker

OMP model execution still reports its pre-existing `401 Incorrect API key` error. This is independent of the memory integration. OMP extension commands, OMP-authored retention, shared-bank provenance, and scoped provider access were verified without relying on the broken model call.
